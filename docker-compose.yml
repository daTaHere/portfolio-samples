name: portfolio-samples
services:
  samples-app:
    build:
      context: .
      dockerfile: Dockerfile
    image: portfolio-samples:1.0
    ports:
      - '5173:5173'
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
